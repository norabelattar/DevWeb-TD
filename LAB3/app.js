import DemoApp from "./modules/DemoApp.js";

document.addEventListener('DOMContentLoaded', () => {
    const app = new DemoApp();
    console.log('Tout est bien initialise');
});

export default DemoApp;